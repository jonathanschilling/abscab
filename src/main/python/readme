This folder (/src/main/python) only contains the Python scripts to create the plots in the article.
The Python implementation of ABSCAB can be found in /abscab,
which is a stand-alone Python module that can be installed via pip.
